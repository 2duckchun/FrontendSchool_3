함수들을 컴포넌트로 만들 수 있음.

리액트를 사용했으면 리액트를 사용한 이유가 있어야함.

리액트는 복잡도가 높음 / 유지보수성이 많이 좋음
뷰는 간단하고 생산성이 빠름

,,,,,,,,,,,,

리악태 으타목 디자인 패턴

배열, 객체 구조분해할당 하기

어레이형식으로 컴포넌트가 들어가면 만들어서 뿌려줄수가 있다.

구분의 역할 : key

DOM은 충분히 빠르다.
그런데 한 1만개를 로드한다?
- DOM으로 컨트롤하기가 힘듦.

그래서 리액트에서는 버츄얼 DOM을 사용함
얘도 DOM은 DOM임. 그러나 혁신적이지는 않음.
아이템이 적으면 적을수록 엇비슷하긴함.

DOM은 수정시 트리를 다시 다그림
버츄얼돔은 key값으로 그림

자바스크립트 cdn 문법?

app
ProductList
ShoppingCart

ProductInfo